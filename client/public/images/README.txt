Drop your images here (e.g. photos, hearts, custom targets).

BACKGROUND:
  Save your background image as:  background.png

TARGETS (each shows as a separate floating target):
  target.png  - Rice crunch/chip (Ugua) - already set up
  chip.png    - Copy ugua_rice_crunch image here for chip targets
  cookie.png  - Copy sfte chocolate chip cookie image here

  Images in the cookie/ subfolder can be copied and renamed.
  The game randomly picks from available images for each target.
